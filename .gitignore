.DS_Store
.playwright-mcp/
.vercel

# arcs update-check runtime state (per-install, never committed)
.arcs-update-required
.arcs-update-stamp

# benchmark run artifacts (keep the harness, not the runs)
bench/results/
bench/run.log
