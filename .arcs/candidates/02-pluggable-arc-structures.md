# pluggable-arc-structures
from: 16-landing-benefits-and-docs-mobile-nav

The arc structure (input → workspace → output) is currently a FIXED default. Make it PLUGGABLE/swappable like plugins — the structure can vary project to project, with the current input/workspace/output as just the default pattern. Then benchmark: run the SAME project through different structures (and a no-arcs baseline) and compare outcomes/quality/speed. Opens a research angle: which structure fits which kind of work. Likely needs structure defs in .arcs/config or a structures/ layer + the CLI scaffolding the chosen one. User will work this next with a separate agent.
