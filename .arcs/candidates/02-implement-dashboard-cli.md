# implement-dashboard-cli
from: 33-arcs-dashboard-design

implement `arcs dashboard` — static viewer per spec (scanner → JSON → lift prototypes/dashboard-v2.html front-end); build as separate arc with a subagent
