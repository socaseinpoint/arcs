# 33-arcs-dashboard-design

goal: design the arcs dashboard — a read-only presentation layer over every .arcs/ on disk; lock the core, prototype it, decide distribution
status: done

## input
- input/brief.md — brainstorm brief: a tooling-layer dashboard to view arcs, lineage, and cross-project activity (kept SEPARATE from arcs core)

## output → pointers
- output/spec.md — the locked design: multi-project model, 5 lenses, board-as-home, digest, distribution = local CLI static viewer
- output/prototype.md — pointer to the working prototypes in ../../../prototypes/ (v0 → v2), what each proves
