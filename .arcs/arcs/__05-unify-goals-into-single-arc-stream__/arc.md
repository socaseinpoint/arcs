# 05-unify-goals-into-single-arc-stream

goal: drop .arcs/goals/; one numbered stream, goal = NN-@slug; descriptive slugs; migrate repo+examples
status: done

## input
input/ask.md — collected requirements (single stream, @ goal marker, arc vs goal, descriptive names).

## output → pointers
- output/result.md — model, CLI changes, migration, docs (parallel subagents), verification.
