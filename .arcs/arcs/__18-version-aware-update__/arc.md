# 18-version-aware-update

goal: version-aware update — nudge on new release, force on breaking
status: done
# closes: <parent-goal checklist-item key this arc completes — optional>
# supersedes: <slug of the arc this one replaces — optional; alias: prev:>

## input
decisions.md — soft-notice always + force on breaking · SemVer tags · git-clone

## output → pointers
- output/result.md — VERSION/MIN_VERSION + arcs version/check-update + hook nudge + gate block
