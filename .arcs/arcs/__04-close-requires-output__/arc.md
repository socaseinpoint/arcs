# 04-close-requires-output

goal: arcs close refuses empty output/ (a closed arc must carry a self-contained result); -f overrides
status: done

## input
input/ask.md — closed arc should have an output file; CLI must enforce it.

## output → pointers
- output/result.md — guard + -f flag, changed files, verification.
