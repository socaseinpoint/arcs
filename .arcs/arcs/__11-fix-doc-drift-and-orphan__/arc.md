# 11-fix-doc-drift-and-orphan

goal: fix doc drift (DEPLOY/examples/template/bin/README) + delete the empty untracked 08 goal-v2 ghost so docs match the CLI's single .arcs/arcs/ stream + STREAM board

status: done

## input
- input/scope.md — 6 fixes (C1 C2 H1 H3 M1 M2) from the arc-10 coherence audit

## output → pointers
- output/result.md — per-item status + every file touched
- workspace/notes.md — verification + diffs against bin/arcs (ground truth)
