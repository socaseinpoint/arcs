# Scope — drop "goal-v2" / legacy framing

User: "goal v2" is internal history nobody needs. Present goals as the single current concept
(immutable contract: goal: line + ## Checklist + supersession). Remove the v1→v2 narrative and the
"no version number / back-compat / falls back to old" comparisons. Prose only — NO behavior change
(the no-checklist→list-sub-arcs fallback is a legit early state, keep the code; just don't frame it
as back-compat). Leave CHANGELOG (it's the historical ledger) and closed .arcs/ history alone.
Surfaces: docs/docs.html, SPEC.md, skill/SKILL.md, README.md, bin/arcs (comments).
