# 01-subagent-delegation

goal: toggleable rules layer; subagents rule (delegate arcs + parallelize) ON by default
status: done

## input
input/ask.md — use subagents/parallelize. workspace/pivot-rules-system.md — reframed to a rules system.

## output → pointers
- output/result.md — rules system + subagents rule, changed files, decisions, verification.
