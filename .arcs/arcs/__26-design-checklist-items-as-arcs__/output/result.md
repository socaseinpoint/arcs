# result — design parked at decision B

Brainstorm for "checklist items as arcs" paused by user mid-design. Captured everything so
nothing is lost; will resume later.

## Outcome
- **Model B decided** (item == sub-arc; `closes:`/keys/`## Checklist` removed). See
  `design-notes.md` (this dir) for the full decision + mechanics sketch.
- **5 open questions left OPEN** (spikes, immutable contract, legacy-goal migration, breaking/
  version) — recorded in `design-notes.md` and inside the candidate so they resurface on resume.
- **Candidate captured:** `07-implement-checklist-items-as-arcs` (from this arc), carrying B +
  the open questions inline.
- **Candidate 04-goal-checklist-actually-fires dropped** — superseded by this design (it was
  "make `closes:` fire"; B deletes `closes:`). Removed by hand (candidate has no drop verb yet —
  roast follow-up).

## Resume path
Promote `07-implement-checklist-items-as-arcs`, answer the 4 open questions, then writing-plans.
