# Result — CHANGELOG.md

Added `CHANGELOG.md` at repo root, Keep a Changelog + SemVer format.

- `[Unreleased]`: rules system + subagents rule, arc close/reopen `__marker__`, numbering
  counts closed, essence-first landing rewrite.
- `[0.1.0] - 2026-06-18`: committed baseline (method, CLI, skill, hooks, i18n, landing).

## Pointers
- New file: `CHANGELOG.md`.

## Caveat
- The `v0.1.0` git tag does not exist yet — the compare/release links 404 until a tag is cut
  on release. Create with `git tag v0.1.0 <baseline-commit>` when releasing.
