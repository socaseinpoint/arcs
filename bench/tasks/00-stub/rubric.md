Score the `add.sh` the candidate produced:

- 5 — correct `add` function + direct-run support; clean, idiomatic, handles
  negatives; no junk files.
- 4 — correct and clean, minor style nits.
- 3 — works for the basic case but sloppy (e.g. no direct-run, hardcoded).
- 2 — partially works / wrong on negatives.
- 1 — missing or broken.
