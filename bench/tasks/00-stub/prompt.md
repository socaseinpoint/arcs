Create a file named `add.sh` in the current directory.

It must define a shell function `add` that takes two integer arguments and prints
their sum to stdout. When the script is run directly with two arguments, it should
print the sum.

Example:
    bash add.sh 2 3   # prints: 5
